# Simple workflow for deploying static content to GitHub Pages
name: Deploy static content to Pages

on:
  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

jobs:
  sync-readme:
    runs-on: ubuntu-latest
    environment: dev #Selection of the Environment
    steps:
      # Check Out the README file to workspace
      - name: Checkout README file
        uses: actions/checkout@v4
        with:
          sparse-checkout: |
            README.md
          sparse-checkout-cone-mode: true

      # Upload to Wiki
      - name: Upload README.md to Wiki.js
        env:
          WIKI_API_TOKEN: ${{ secrets.WIKI_API_TOKEN }}
          WIKI_API_URL: ${{ vars.WIKI_API_URL }}
          WIKI_PAGE_ID: ${{ vars.WIKI_PAGE_ID }}
          path: "Pedro/nova-pagina"
        run: |
          echo "🔍 Reading README.md content..."
          CONTENT=$(cat README.md | jq -Rs .)
          echo "✅ README content loaded."

          echo "🔧 Preparing GraphQL query..."
          
          GRAPHQL_QUERY=$(cat <<'EOF'
          mutation UpdatePage($id: Int!, $content: String!, $path: String!) {
            pages {
              update(
                id: $id,
                content: $content,
                description: "README atualizado",
                editor: "markdown",
                isPrivate: false,
                isPublished: true,
                locale: "en",
                path: $path,
                tags: ["updated"]
              ) {
                responseResult {
                  succeeded
                  slug
                  message
                }
              }
            }
          }
          EOF
          )
          echo "✅ GraphQL query prepared."

          echo "📦 Building JSON payload..."
          curl -X POST "$WIKI_API_URL" \
            -H "Authorization: Bearer $WIKI_API_TOKEN" \
            -H "Content-Type: application/json" \
            -d "$(jq -n \
              --arg query "$GRAPHQL_QUERY" \
              --argjson variables "{\"id\": $WIKI_PAGE_ID, \"content\": $CONTENT}" \
              '{query: $query, variables: $variables}')"
          echo "✅ JSON payload built:"
          echo "$JSON_PAYLOAD"
          
          echo "🚀 Sending request to Wiki.js API..."

